<nav class="sticky-top mx-0 navbar navbar-expand-lg navbar-light bg-light">
    <a href="/" class="navbar-brand" id = "<%=path==='index'?'selected':''%>" >HOME</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        
        <li class="nav-item active">
          <a class="nav-link" id="<%=path === 'get-covid' ? 'selected':''%>" href="/get-covid">COVID SECTION<span class="sr-only">(current)</span></a>
        </li>

        <li class="nav-item active">
          <a class="nav-link" id="<%=path === 'get-quarantine-info' ? 'selected':''%>" href="/quarantine-info">GET QURANTINE INFO <span class="sr-only">(current)</span></a>
        </li>
        <%if(isAuth && isAuth.isAdmin){%>
        <li class="nav-item active">
            <a class="nav-link" id="<%=path === 'add-quarantine-info' ? 'selected':''%>" href="/add-quarantine-info">ADD QURANTINE INFO <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item active">
          <a class="nav-link" id="<%=path === 'get-reported-info' ? 'selected':''%>" href="/get-reported-info">GET REPORTED INFO <span class="sr-only">(current)</span></a>
      </li>
        <%}%>
        <%if(isAuth && isAuth.isAdmin && isAuth.isMainAdmin){%>
          <li class="nav-item active">
              <a class="nav-link" id="<%=path === 'manage-users' ? 'selected':''%>" href="/manage-users">MANAGE USERS <span class="sr-only">(current)</span></a>
          </li>
          <%}%>
      </ul>
   
      <ul class = "navbar-nav">
        
        <%if(!isAuth){%>
        <li class="nav-item active">
          <a class="nav-link" id="<%=path === 'login' ? 'selected':''%>" href="/login">LOG IN<span class="sr-only">(current)</span></a>
        </li>

        <li class="nav-item active">
            <a class="nav-link" id="<%= path === 'signup' ? 'selected' : ''%>" href="/signup">SIGN UP<span class="sr-only">(current)</span></a>
        </li>
        <%}else{%>
        <li class="nav-item active">
          <a class="nav-link" id="" href="/logout">Log Out<span class="sr-only">(current)</span></a>
        </li>
        <%}%>
      </ul>
    </div>
  </nav>
